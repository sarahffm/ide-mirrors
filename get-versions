#!/usr/bin/env bash

software="az"
versions_url="./${software}/versions-url"
versions_url_op="./${software}/versions-url-op"
echo "$versions_url, $versions_url_op"

# for software in */ ;
# do
#   software=${software::-1}
#   versions_url=$(cat "./${software}/versions-url")
#   versions_url_op=$(cat "./${software}/versions-url-op")

#   # TODO: check if there are subdirectories in ./<<software>>
#   # if no:
#   if [ "${versions_url_op}" == "github" ]
#   then
#     curl -s "${versions_url}" | awk -F "\"" '/tag_name/ {print $4}' | sort -V -r > "./${software}/available_versions"
#   else 
#     eval "curl -s "${versions_url}" | "${versions_url_op}"" > "./${software}/available_versions"
#   fi

# if yes:
